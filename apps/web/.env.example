# Default: /api (the reverse proxy removes this prefix before forwarding to FastAPI).
# For a direct API connection, use its base URL, for example http://localhost:8000.
VITE_API_BASE_URL=/api
# Target used only by the Vite development proxy.
VITE_API_PROXY_TARGET=http://localhost:8000
